services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: instagram_frontend
    ports:
      - "5173:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always
