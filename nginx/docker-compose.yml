services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: instagram_nginx_gateway
    restart: always
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
